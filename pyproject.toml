[project]
name = "filmulator-gui"
version = "0.11.1"
description = "A Qt Quick GUI film emulator for RAW photo processing"
dependencies = [
    "clang-format==18.*",
    "clang-tidy==18.*",
]

[tool.uv]
dev-dependencies = [
    "pytest",
]
